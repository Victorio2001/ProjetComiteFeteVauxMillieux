<form method="post" class="mt-2" action="{{ path('app_manifestation_delete', {'id': manifestation.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette manifestation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ manifestation.id) }}">
    <button class="btn btn-danger">Supprimer l'élement</button>
</form>
