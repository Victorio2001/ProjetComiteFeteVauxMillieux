<form method="post" class="mt-2" action="{{ path('app_materiel_delete', {'id': materiel.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce matériel?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel.id) }}">
    <button class="btn btn-danger">Supprimer l'élement</button>
</form>
